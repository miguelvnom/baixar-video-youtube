export const metadata = {
  title: "Baixar Vídeo do YouTube",
  description: "Ferramenta simples para baixar vídeos do YouTube",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
